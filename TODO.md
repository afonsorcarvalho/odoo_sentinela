# TODO — Sentinela

## Em curso
- <nada ainda>

## Pendente
- **[INTEGRIDADE] Lastro entre linha do Timescale e arquivo assinado do coletor** — hoje NÃO existe para leituras. A ECDSA é verificada na ingestão (`ingestao/validador.py:143-165`) mas a prova é volátil: o `hash` por linha é descartado (`ingestao/timescale.py:9-31`), o `.txt` bruto não é arquivado, e `sensor_reading` não tem hash/assinatura/FK. Só sobra o hash agregado diário no `file.ledger` — não amarra nenhuma leitura específica ao arquivo. Alvo (rastreável por linha): **(A)** arquivar o `.txt` assinado referenciado pelo ledger + **(B)** carimbar `sensor_reading` com `ledger_id` (FK) + hash encadeado da linha. (A) sozinho já dá lastro reverificável. Fora de escopo agora: chave pública no Odoo (hoje em JSON local gitignored), encadeamento inter-dia real, e coabitação de dados demo/backfill não-assinados na mesma tabela. Diagnóstico completo em [plans/vamos-discutir-sobre-como-precious-whisper.md](../../.claude/plans/vamos-discutir-sobre-como-precious-whisper.md). Semente de spec futura em `docs/superpowers/specs/`.
- **[BLOQUEIA PRODUÇÃO] `dashboard_config` usa `SITE_CODE` fixo por processo, não o site do usuário logado** (`api/config.py:20-25`). Pressupõe "um processo por site"; incompatível com instância única multi-tenant (o que a spec de isolamento multi-tenant está corrigindo pro resto da API). Usuário de outro site recebe layout/config que não é dele → dashboard quebra (widgets referenciando `sensor_code` alheio). Resolver: derivar site a partir do usuário logado (via sites permitidos), não de env var fixa. Achado durante brainstorm de [2026-07-20-api-multitenant-isolation-design.md](docs/superpowers/specs/2026-07-20-api-multitenant-isolation-design.md), fora do escopo dela.
- **Extrair `resolveAreaCodes(binding)`** (follow-up multi-área). A fórmula `areaCodes ?? (areaCode ? [areaCode] : [])` está duplicada em `registry.tsx` e `WidgetConfigPopover.tsx` — extrair helper compartilhado p/ evitar divergência silenciosa. Minor.
- **Freshness em KpiWidget + SensorDetailDrawer** (follow-up da A2). Hoje coloram 1 sensor pelo `alarm_state` cru, sem freshness — um sensor offline lê "verde/OK" nesses widgets enquanto o AreaCard do mesmo sensor mostra offline. Mesma classe "silêncio lido como OK" que a A2 matou no AreaCard, sobrevivendo noutra superfície. Estender `freshness()` a esses dois widgets.
- **A2 fase 2 — fechar o gap "morto no page-load"** (backend). Precisa de `last_seen_ts` autoritativo por sensor (no snapshot/meta) e/ou consumir o alarme `sensor_offline` do backend, para detectar sensor já morto antes da carga (a fase 1, client-side por idade, só pega degradação observada + escala `never` após graça de 10min). Ver Dependências §3 da spec da A2.
- **Thresholds de freshness configuráveis** (`stale_ms`/`offline_ms` via `DashboardConfig`, globais e/ou por sensor) — hoje fixos em `lib/freshness.ts` (5/15 min). Idealmente derivados da cadência de amostragem esperada.
- **[DISCUSSÃO FUTURA] Suporte a cliente com mais de um site (multi-site por usuário)**. `obter_sites_permitidos` (spec de isolamento multi-tenant) já pode retornar vários sites por usuário, mas não há seletor/troca de site no frontend nem definição de qual site vira "ativo" por padrão pro dashboard. Levantado como fora de escopo em [2026-07-20-api-multitenant-isolation-design.md](docs/superpowers/specs/2026-07-20-api-multitenant-isolation-design.md).

## Feito
- 2026-07-20 — **Polish do filtro de alarmes**. Chip "Todas" (toggle-all, 3 estados via `aria-pressed`: true/mixed/false, com visual próprio pro mixed) → isolar uma área = 2 toques, voltar a todas = 1 toque; chips ~50% menores no visual mantendo alvo de toque ≥44px (`min-h-11`/`min-w-11`, pill interno `text-[11px]`). Padrão escolhido após pesquisa de UX (Plotly/ECharts legend, Metabase, WCAG target-size). Mergeada (`8ac5913`). Spec: [2026-07-20-alarme-filtro-polish-design.md](docs/superpowers/specs/2026-07-20-alarme-filtro-polish-design.md).
- 2026-07-20 — **Filtro de áreas ao vivo no card de alarmes**. Chips toggle (universo = união áreas-de-sensores + áreas-dos-alarmes) filtram quais áreas mostrar; config = default; filtro de sessão (reseta no reload). Review pegou 1 Critical (all-clear falso se `useSensors` degrada) → corrigido (união garante que alarme nunca fica invisível). Mergeada (`2d2817e`). Spec: [2026-07-20-alarme-filtro-area-runtime-design.md](docs/superpowers/specs/2026-07-20-alarme-filtro-area-runtime-design.md).
- 2026-07-20 — **Escopo de alarme por múltiplas áreas**. `AlarmsWidget` scope='área' passa a aceitar várias áreas (`binding.areaCodes[]`); UI dropdown "adicionar área" + chips removíveis no popover; backward-compat com `areaCode` legado; vazio→site. Mergeada (`f7252c9`). Spec: [2026-07-20-alarme-multi-area-design.md](docs/superpowers/specs/2026-07-20-alarme-multi-area-design.md).
- 2026-07-20 — **A2 — Indicador de sensor offline / dado velho** (dashboard). freshness puro + relógio `useNow` + agregação `offline→≥warn` (sensor parado nunca deixa a área verde) + visual (badge/`—`/marcador de offline). Fase 1 (client-side); prereq de unidade do `ts` resolvido (backend manda ms) e cadência validada (~1/min). Mergeada em master (`0fbf89b`). Spec: [2026-07-19-sensor-offline-stale-data-design.md](docs/superpowers/specs/2026-07-19-sensor-offline-stale-data-design.md).
- 2026-07-20 — **D3 — Drill-down de widget → detalhe do sensor**. Religou o `SensorDetailPanel` (código morto) via drawer lateral + fix de altura DT/M2. Mergeada.
- 2026-07-20 — **B2 — Config por widget via UI** (janela do gráfico, threshold do KPI, escopo do alarme). Mergeada.
- 2026-07-20 — **A3 — Reconexão SSE visível** (badge AO VIVO reflete estado da conexão). Mergeada.
- 2026-07-20 — **A1 — Error boundary por widget** (widget quebrado não derruba o dashboard). Mergeada.
