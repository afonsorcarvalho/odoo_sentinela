# TODO — Sentinela

## Em curso
- <nada ainda>

## Pendente
- **A2 — Indicador de sensor offline / dado velho** (dashboard). Spec: [2026-07-19-sensor-offline-stale-data-design.md](docs/superpowers/specs/2026-07-19-sensor-offline-stale-data-design.md). BLOQUEADA por pré-requisitos de backend: mock emite `ts` de 2023 (tudo apareceria offline), unidade do `ts` no backend real não verificada, e fechar o gap real precisa de `last_seen_ts`/alarme `sensor_offline` autoritativo do backend.

## Feito
